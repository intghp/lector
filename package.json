{
  "name": "lector-monorepo",
  "scripts": {
    "infra:up": "docker-compose up -d",
    "dev:backend": "cd backend && source venv/bin/activate && uvicorn app.main:app --reload --port 8000",
    "dev:mobile": "cd mobile && npx expo start",
    "dev": "npm run infra:up && concurrently -c \"blue,green\" -n \"FASTAPI,EXPO\" \"npm run dev:backend\" \"npm run dev:mobile\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}